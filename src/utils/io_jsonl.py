def load_jsonl(path):`n    """Load JSONL file."""`n    from pathlib import Path`n    import json`n    results = []`n    with open(Path(path), "r", encoding="utf-8") as f:`n        for line in f:`n            line = line.strip()`n            if line and not line.startswith("#"):`n                try:`n                    results.append(json.loads(line))`n                except:`n                    continue`n    return results
